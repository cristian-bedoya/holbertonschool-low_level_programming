#include "holberton.h"

/**
 * print_sign -  print the last digit
 *
 * @n: n is integer
 * Return: 1 +, cer0 , -1.
 */

int print_last_digit(int n)
{
	n = n % 10;

	if ( n < 0)
	{
		n = -n;
	}
	_putchat(n + 48;
	return (n);
}
